var Composer = {
  initialize: function () {
    this.window = null;
  },

  toggleTarget: function (target) {
    alert('toggleTarget(' + target + ')');
  },

  loadURL: function (url) {
    alert('loadURL(' + url + ')');
  }
};
